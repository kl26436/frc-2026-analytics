import FuelCalibration from '../FuelCalibration';

// Wraps the existing FuelCalibration page so it lives inside the admin tab shell.
export default function CalibrationTab() {
  return <FuelCalibration />;
}
